# AI Agent System: Подробное описание реализации

Добро пожаловать в проект **AI Agent System**! Это современная система на базе Laravel и AI SDK, построенная по принципу "мульти-агентности" (Multi-Agent System). Она позволяет решать сложные задачи, разбивая их на мелкие шаги, выбирая нужные инструменты и даже "обдумывая" промежуточные результаты.

Данный документ поможет вам быстро разобраться в том, как всё устроено внутри, какие классы за что отвечают и как работает магия ИИ-агентов.

---

## 1. Общая архитектура системы

Система построена на **событийно-ориентированном подходе (Event-Driven Architecture)**. Это значит, что вместо одного длинного и жесткого программного кода, выполнение задачи происходит через цепочку событий.

### Почему это удобно?
- **Гибкость**: Вы можете легко добавить новое действие (например, отправку письма или логирование в Telegram), просто подписавшись на нужное событие.
- **Прозрачность**: На каждом этапе мы точно знаем, что делает агент: "планирует", "ищет информацию", "считает" или "отвечает".

### Ключевые события (Events):
- **`UserMessageReceived`**: Точка входа. Пользователь отправил сообщение.
- **`PlanCreated`**: Система составила план действий.
- **`ToolCalled`**: Вызван конкретный инструмент (например, калькулятор).
- **`ReflectionGenerated`**: Агент проанализировал результат и решил, что делать дальше.
- **`StepCompleted`**: Текущий шаг задачи завершен.

---

## 2. Основные компоненты (Мозги системы)

### Supervisor (Супервизор) — Главный оркестратор
Супервизор — это "начальник", который принимает запрос от пользователя и решает, каких подчиненных (агентов) привлечь для решения.
- **Класс**: `App\Ai\Core\Supervisor`
- **Метод**: `handle(string $userMessage)` — принимает сообщение и запускает процесс.

### Hybrid Planner — Планировщик стратегий
Планировщик решает, как именно мы будем выполнять задачу. Он "гибридный", потому что сочетает в себе два подхода:
1. **Правила (Deterministic Rules)**: Если запрос простой (например, "сделай резюме"), мы сразу знаем, что нужно вызвать `ResearchAgent` и `SummaryAgent`. Это быстро и дешево.
2. **ИИ-планирование (LLM Plan)**: Если задача сложная и уникальная, мы просим нейросеть (LLM) составить план действий.
- **Класс**: `App\Ai\Core\HybridOrchestratorPlanner`
- **Методы**: `plan()` — выбирает стратегию; `llmPlan()` — запрашивает план у ИИ.

### Orchestration Executor — Исполнитель плана
Этот компонент берет готовый план (список шагов) и по очереди вызывает нужных агентов, передавая результат одного другому.
- **Класс**: `App\Ai\Core\OrchestrationExecutor`
- **Метод**: `execute(OrchestrationPlan $plan, string $initialInput)` — запускает цикл выполнения.

---

## 3. Специализированные агенты

В системе есть несколько типов агентов, каждый из которых хорош в своём деле:

### ResearchAgent (Исследователь)
Умеет искать информацию в базе знаний и выполнять расчеты. Он работает внутри цикла (Loop), пока не найдет ответ.
- **Класс**: `App\Ai\Agents\ResearchAgent`
- **Особенность**: Использует `LoopController` для итеративного поиска.

### SummaryAgent (Саммаризатор)
Специализируется на сжатии и структурировании информации. Получает "кучу данных" и превращает их в краткий отчет.
- **Класс**: `App\Ai\Agents\SummaryAgent`

---

## 4. Инструменты и База знаний (RAG)

Агенты не просто "галлюцинируют", они пользуются реальными инструментами:

### Реестр инструментов (Tool Registry)
Позволяет динамически добавлять новые навыки агентам.
- **Класс**: `App\Ai\Core\ToolRegistry`
- **Методы**: `register()` — добавляет инструмент; `getTools()` — отдает список для ИИ.

### Реализованные инструменты:
- **`CalculatorTool`**: Для точных математических вычислений.
- **`VectorSearchTool`**: Для поиска по загруженным документам.

### Поиск по знаниям (RAG - Retrieval-Augmented Generation)
Система векторизует (превращает в числа) тексты документов и сохраняет их в базу данных PostgreSQL с расширением `pgvector`. Когда агент ищет информацию, он сравнивает "смысл" запроса со "смыслом" фрагментов текста в базе.
- **Класс**: `App\Ai\Memory\VectorStore`
- **Метод**: `search()` — выполняет семантический поиск.

---

## 5. Алгоритм работы (Как проходит запрос)

Когда вы пишете: *"Найди информацию о прибыли компании за 2023 год и сделай краткий вывод"*, происходит следующее:

1. **Планирование (`Supervisor` + `Planner`)**:
   - `Supervisor::handle()` принимает текст.
   - `HybridOrchestratorPlanner::plan()` видит слово "вывод" (или "резюме") и строит план:
     - Шаг 1: `research` (Исследователь соберет цифры).
     - Шаг 2: `summary` (Саммаризатор сделает вывод).

2. **Выполнение (`Executor` + `AgentState`)**:
   - Создается `AgentState` — это "чемоданчик" с данными, который агенты передают друг другу.
   - Вызывается `ResearchAgent::execute()`:
     - Он запускает `LoopController`.
     - `LoopController` вызывает `VectorSearchTool` для поиска прибыли в базе.
     - `Reflector` (Рефлексия) проверяет результат: "Нашли данные за 2023? Да, достаточно. Заканчиваем поиск".
   - Результат поиска кладется в `AgentState`.
   - Вызывается `SummaryAgent::execute()`:
     - Он берет данные из `AgentState` и пишет итоговый текст.

3. **Результат**:
   - `Supervisor` отдает вам готовый ответ.

---

## 6. Технические особенности для разработчиков

- **Reasoning Stream (Ход мыслей)**: Вы можете наблюдать за процессом работы в реальном времени через веб-интерфейс (Server-Sent Events). Каждое событие (вызов инструмента, решение рефлектора) мгновенно отображается на фронтенде.
- **Guardrails (Безопасность)**: В `OrchestrationExecutor` встроены проверки. Если ИИ предложит вызвать несуществующего агента, система заблокирует этот шаг.
- **Docker и Окружение**:
  - PHP 8.4-cli.
  - PostgreSQL + pgvector для памяти.
  - Redis для очередей и событий.
  - Ollama для локального запуска LLM моделей (например, `llama3`, `mistral`).
- **Тестирование**: Система покрыта тестами (`MultiAgentTest.php`, `LoopControllerTest.php`), которые проверяют как отдельных агентов, так и их совместную работу.

---

## 7. Веб-интерфейс (Тестовый фронтенд)

Для удобства реализована страница (`AiController`), где можно протестировать разные режимы:
- **Обычный (ask)**: Просто дождаться ответа.
- **Стрим (stream)**: Видеть "ход мыслей" и печатающийся текст в реальном времени.
- **Очередь (queue)**: Отправить тяжелую задачу в фон.

---
Эта архитектура делает систему предсказуемой, расширяемой и очень мощной в решении нетривиальных бизнес-задач.
